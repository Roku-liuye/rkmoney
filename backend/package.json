{
  "name": "backend",
  "version": "1.0.0",
  "description": "rkmoney FastAPI backend",
  "main": "main.py",
  "scripts": {
    "dev": "uvicorn main:app --reload --host 0.0.0.0 --port 8000",
    "start": "uvicorn main:app --host 0.0.0.0 --port 8000",
    "migrate": "python -c \"from database import engine; from models import Base; Base.metadata.create_all(bind=engine)\""
  },
  "keywords": [],
  "author": "",
  "license": "MIT"
}
